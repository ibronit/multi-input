# Multi-input react app

This is a demo app where you can add/remove inputs to the form dynamically. It is available [here](https://multi-input.herokuapp.com/).

### Bootstrap the app

```sh
npm install
npm start
```